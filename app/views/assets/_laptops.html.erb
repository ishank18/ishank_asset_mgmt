<table>
	<tr>
		<td>Operating System <%= content_tag(:span, MANDATORY, :class => "Mandatory") %></td>
		<td>
			<%= text_field_tag "asset[resource_attributes][operating_system]", @asset.try(:resource).try(:operating_system) %>
		</td>
	</tr>
	<tr>
		<td>Has Bag</td>
		<td>
				<%= hidden_field_tag "asset[resource_attributes][has_bag]", false %>
				<%= check_box_tag "asset[resource_attributes][has_bag]", true, @asset.try(:resource).try(:has_bag) %>
		</td>
	</tr>
</table>	

