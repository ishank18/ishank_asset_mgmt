<table>
	<tr>
		<td>Operating System</td>
		<td>
			<% if !@asset.nil? %>
				<%= text_field_tag "resource[operating_system]", @asset.resource.operating_system %>
			<% else %>
				<%= text_field_tag "resource[operating_system]" %>
			<% end %>	
		</td>
	</tr>
</table>
